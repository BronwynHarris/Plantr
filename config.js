const config = {
    database: 'plantr',
    username: 'fsa',
    password: 'secretPassword123',
    config: {
        host: 'localhost',
        dialect: 'postgres'
    }
}

module.exports = config;
